# 写入工单备料提示优化需求分析

## 1. 需求背景

当前“写入工单备料”完成后，页面统一显示“成功 X 行，失败 Y 行；失败原因……”。
当备料新增已经成功，但原备料清理失败时，提示会把业务成功和清理异常混在一起，用户无法判断当前工单是否已经写入，也无法判断是否可以再次执行。

本次只优化结果提示的表达方式，不改变欠料计算、备料写入、原备料清理和事务处理规则。

## 2. 优化目标

1. 优先告诉用户工单备料是否已经写入。
2. 区分“写入成功”“写入失败”和“原备料清理失败”。
3. 成功场景使用简短、明确的业务语言。
4. 部分成功场景说明已完成和未完成的数量，并提示用户下一步。
5. 技术异常保留在详细信息中，避免技术堆栈直接作为主提示。
6. 保留目标生产订单号和最近更新时间，便于追溯。

## 3. 建议提示文案

### 3.1 全部成功

```text
工单备料已更新
目标订单：QT0001
本次写入：1 行
更新时间：2026-08-28 08:02:51
```

### 3.2 写入部分成功

```text
工单备料已部分更新
目标订单：QT0001
已写入：1 行，未写入：1 行
请检查未写入明细。
更新时间：2026-08-28 08:02:51
```

### 3.3 写入成功，但原备料清理失败

这是本次截图对应的重点场景，建议单独提示：

```text
工单备料已写入，但原备料清理未完成
目标订单：QT0001
本次写入：1 行
请暂勿重复执行，先处理原备料清理问题。
更新时间：2026-08-28 08:02:51
```

详细信息中再显示：

```text
原备料清理失败：请设置关联的料品
```

### 3.4 全部失败

```text
工单备料未完成
目标订单：QT0001
已写入：0 行，失败：1 行
请检查失败明细后重试。
更新时间：2026-08-28 08:02:51
```

### 3.5 未找到欠料数据

```text
当前没有可写入的欠料数据，未修改工单备料。
```

## 4. 结果分类规则

提示分类按以下顺序判断：

1. 没有欠料数据：提示未修改目标订单。
2. 写入行数为 0：提示工单备料未完成。
3. 写入成功且原备料清理成功、无失败明细：提示工单备料已更新。
4. 写入成功但存在普通失败行：提示工单备料已部分更新。
5. 写入成功但存在“原备料清理失败”：优先提示“工单备料已写入，但原备料清理未完成”，不能只显示“失败 X 行”。

## 5. 展示方式建议

1. 主提示使用短句和换行，控制在结果区域内，避免截图中长文本挤压表格。
2. 目标订单、写入数量、失败数量、更新时间作为固定摘要字段展示。
3. 失败原因作为“详细信息”展示；主提示不显示内部异常类型、堆栈或过长技术信息。
4. 清理失败时明确提示“请暂勿重复执行”，避免重复产生备料行。
5. 不改变现有成功/失败计数和日志记录，仅调整用户看到的文案和分类。

## 6. 本次不涉及

- 不改变欠料统计规则。
- 不改变目标生产订单选择规则。
- 不改变先清空再写入的业务规则。
- 不改变部分保存策略。
- 不自动删除 QT0001 当前已有数据。
- 不增加新的确认弹窗。

## 7. 待确认事项

请确认是否采用以上文案和分类规则。确认后再开始修改 UI 提示、编译、部署和页面验证。
