# 委外欠料统计需求分析

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 功能名称 | 委外欠料统计 |
| 业务入口 | 生产制造 -> 生产管理 -> 齐套分析 |
| 所属界面 | `MFG.MO.StartAnalysisUI` |
| 相关现有功能 | 【缺料表】 |
| 当前状态 | 需求规则已确认，等待开始实现授权 |

## 2. 需求目标

在齐套分析界面现有【缺料表】按钮前增加【委外欠料统计】按钮。

用户点击按钮后，系统按当前组织查询委外采购订单和供应链备料数据，计算各物料的未领数量，按照供应地点扣减库存，得到实际欠料数量，并在委外欠料表中展示。用户在该表单点击【写入工单备料】后，再将欠料结果写入当前组织单据类型编码为 `QT01` 的第一张生产订单备料中。

本阶段只整理需求，不创建代码、不修改数据库、不部署系统。

## 3. 页面入口与按钮位置

### 3.1 按钮名称

```text
委外欠料统计
```

### 3.2 按钮位置

- 页面：齐套分析主界面。
- 位置：现有【缺料表】按钮前面。
- 两个按钮使用同一齐套分析 UI 插件注册和同一工具栏。
- 按钮应使用幂等添加逻辑，避免齐套分析外层 WebPart 和内层 WebPart 同时初始化时重复显示。

### 3.3 点击后的交互方式

已确认采用两步操作：

1. 在齐套分析界面点击【委外欠料统计】，生成并打开委外欠料表。
2. 用户核对结果后，在委外欠料表点击【写入工单备料】，再执行目标工单备料清空和写入。

打开委外欠料表本身不修改生产订单备料。

## 4. 数据范围

### 4.1 组织范围

- 只处理当前登录组织。
- 采购订单、供应链备料、库存和目标生产订单必须使用同一当前组织范围。
- 不能跨组织读取、扣减库存或写入生产订单。

### 4.2 采购订单筛选

采购订单行候选条件：

| 条件 | 规则 |
| --- | --- |
| 组织 | 当前组织 |
| 采购订单业务类型 | 编码等于 `PM055` |
| 采购订单行状态 | 等于 `0`、`1` 或 `2` |

采购订单头、采购订单行以及计划行的实际实体和字段名，需要在实现前根据当前 U9CE 元数据确认。

### 4.3 供应链备料筛选

相关实体：

```text
UFIDA::U9::CBO::SCM::PickList::SCMPickList
```

供应链备料字段和计算规则：

| 字段 | 规则 |
| --- | --- |
| `ActualReqQty` | 实际需求数量 |
| `IssuedQty` | 已领数量，空值按 0 处理 |
| 未领数量 | `ActualReqQty - IssuedQty` |
| `SupplyWh` | 供应地点，用于匹配库存仓库 |

只有未领数量大于 0 的记录进入后续统计。

已确认关联链路为：

```text
供应链备料 -> 供应链备料头 -> 采购订单行
```

后续实现按该业务关系取得采购订单行，不采用仅按料品编码的模糊关联。

统计维度已确认为：

```text
采购订单供应商 + 采购订单行 + 供应链备料头 + 供应链备料行
```

不同供应商、采购订单行、备料头或备料行分别保留统计结果，不跨这些维度合并。

## 5. 欠料计算规则

### 5.1 基础未领数量

对符合条件的供应链备料记录计算：

```text
未领数量 = ActualReqQty - IssuedQty
```

计算结果小于或等于 0 的记录不作为欠料候选。

### 5.2 按供应地点扣减库存

- 当 `SupplyWh` 有值时，按供应地点匹配对应仓库的现存量。
- 库存扣减逻辑为：

```text
欠料数量 = max(未领数量 - 可扣减现存量, 0)
```

- 同一物料、同一供应地点存在多条备料记录时，按日期正序、行号正序共享扣减库存，不能每行重复使用同一份库存。
- 当 `SupplyWh` 为空时，不进行库存匹配和扣减，原则上该记录的未领数量直接作为欠料数量。
- 最终只有欠料数量大于 0 的结果进入欠料清单。

### 5.3 库存范围

已确认库存口径为采购现存量。按当前组织、料品和 `SupplyWh` 仓库匹配采购现存量，并按确认的顺序共享扣减。

采购现存量与供应链备料计量单位不一致时，使用 U9 标准单位换算规则转换到可比较的数量口径。

## 6. 欠料清单建议字段

首版建议至少保留以下字段，便于核对和写入：

- 当前组织
- 委外采购订单号
- 委外采购订单行号
- 生产订单号或来源生产订单
- 供应链备料主键或行号
- 料品编码
- 料品名称
- 需求单位
- 供应地点编码
- 供应地点名称
- `ActualReqQty`
- `IssuedQty`
- 未领数量
- 扣减库存数量
- 最终欠料数量

已确认需要独立表单展示统计结果，并提供【写入工单备料】按钮。表单首版展示字段以核对统计结果为目标，至少包括供应商、采购订单号及行号、备料行、料品、供应地点、实际需求数量、已领数量、采购现存量扣减数量和欠料数量；同时支持 Excel 导出。

## 7. 写入目标生产订单

### 7.1 目标订单筛选

从当前组织生产订单中筛选：

```text
生产订单单据类型编码 = QT01
```

选择符合条件的第一张生产订单作为写入目标，按生产订单日期正序确定“第一张”。具体日期字段名称在实现阶段按当前 U9CE 元数据确认，不能使用数据库无序结果。

如果按日期正序取得的第一张 `QT01` 生产订单不存在、状态不允许修改或无法清空和新增备料，系统直接报错提示，不自动继续查找下一张生产订单。

### 7.2 写入目标

将欠料清单写入目标生产订单的备料明细中。

已确认写入方式为：

1. 用户在委外欠料表点击【写入工单备料】。
2. 先清空目标生产订单现有备料明细。
3. 按本次统计结果重新新增备料行。
4. 不与原有备料行合并，不保留目标订单原有备料行。

### 7.3 写入数量

新增工单备料行只要求写入以下业务字段：

- 料号
- 实际需求数量

实际需求数量取扣减采购现存量后的最终欠料数量：

```text
工单备料.ActualReqQty = 欠料数量
```

若 U9 标准保存校验还要求系统必填字段，则按目标生产订单和料品默认规则补齐，不增加额外业务输入字段。

### 7.4 保存与事务

- 点击【委外欠料统计】只执行统计和展示；点击【写入工单备料】后直接执行写入，不再增加二次确认弹窗。
- 采用部分保存策略：成功处理的行可以保留，失败行单独记录并提示。
- 写入前需要校验目标生产订单是否允许修改。
- 写入完成后提示成功数量、失败数量、失败原因和最近更新时间。
- 提示框需要显示本次处理结果的最近更新时间，具体显示格式在实现阶段统一。

## 8. 重复执行控制

同一用户可以重复执行统计。每次点击【写入工单备料】时，按“先全部清空，再按当前表单统计结果新增”的规则处理，不在目标订单中累加上次结果。

每次执行完成后，提示框显示本次处理的最近更新时间，便于用户判断当前 QT01 生产订单备料数据对应的统计批次。

## 9. 异常处理

以下情况需要明确提示并记录日志：

- 当前组织为空或无法识别。
- 当前组织没有符合 `PM055` 的采购订单行。
- 供应链备料记录找不到对应采购订单关系。
- `ActualReqQty` 或 `IssuedQty` 为空、负数或单位不一致。
- `SupplyWh` 为空时不扣库存，但仍保留欠料数量。
- 找不到对应库存仓库。
- 找不到当前组织 `QT01` 生产订单。
- 目标生产订单已审核、已关闭或不允许修改。
- 备料行必填字段无法补齐。
- 写入部分失败或事务回滚。
- 没有欠料数据时，只显示提示信息，不清空或修改目标生产订单备料。

## 10. 建议处理流程

1. 获取当前组织。
2. 查询当前组织、业务类型 `PM055`、采购订单行状态 `0/1/2` 的采购订单行。
3. 查询与委外采购订单业务相关的 `SCMPickList` 供应链备料记录。
4. 计算每条备料的未领数量 `ActualReqQty - IssuedQty`。
5. 按料品、供应地点及确认后的关联主键归集未领数量。
6. 对有 `SupplyWh` 的记录匹配库存并按顺序扣减；无 `SupplyWh` 的记录不扣减。
7. 筛选最终欠料数量大于 0 的记录形成欠料清单。
8. 在委外欠料表展示结果，并支持 Excel 导出。
9. 用户点击【写入工单备料】后，查询当前组织单据类型 `QT01` 的目标生产订单，并按日期正序取第一张。
10. 校验目标生产订单状态和备料行必填字段；不允许修改时直接报错，不查找下一张。
11. 清空目标生产订单原备料行，再按当前统计结果新增料号和实际需求数量。
12. 采用部分保存策略，提示成功行、失败行、失败原因及最近更新时间。

## 11. 已确认规则汇总

以下规则已经确认：

1. **采购订单与供应链备料的关联方式**：已确认按“供应链备料 -> 供应链备料头 -> 采购订单行”关联。
2. **统计粒度**：已确认按“采购订单供应商 + 采购订单行 + 供应链备料头 + 供应链备料行”分别统计，不跨维度合并。
3. **库存口径**：已确认使用采购现存量。采购现存量是否已经包含冻结、占用、预留等扣减，由该标准库存口径本身决定。
4. **库存扣减顺序**：已确认按日期正序、行号正序扣减。
5. **单位换算**：已确认使用 U9 标准单位换算规则。
6. **目标 QT01 订单的“第一张”定义**：已确认按生产订单日期正序取第一张，具体日期字段按 U9 元数据确认。
7. **目标订单状态**：按 U9 标准可编辑状态校验；第一张不允许修改时直接报错提示，不继续找下一张。
8. **备料写入方式**：已确认先全部清空目标生产订单备料，再按本次结果新增备料行。
9. **备料行字段**：已确认业务字段为料号、实际需求数量；标准保存要求的系统字段按默认规则补齐。
10. **写入数量**：已确认 `ActualReqQty = 欠料数量`。
11. **重复执行**：已确认每次先清空目标备料，再按本次统计结果新增，不重复累加历史结果。
12. **是否预览确认**：已调整为先展示委外欠料表，再由用户点击【写入工单备料】；点击写入按钮后直接执行，不增加二次确认。
13. **结果展示**：已确认需要统计结果表单、【写入工单备料】按钮和 Excel 导出，并在处理后提示目标工单、成功行数、失败行数及最近更新时间。
14. **无数据处理**：已确认只显示提示信息，不创建、清空或修改 `QT01` 生产订单备料。
15. **失败处理**：已确认部分保存，成功行保留，失败行单独提示并记录原因。
16. **权限范围**：暂不增加单独的按钮功能权限控制，仍受当前组织数据范围和 U9 标准生产订单保存校验约束。

## 12. 实现前验收口径

需求确认后，至少应验证：

1. 齐套分析界面中【委外欠料统计】位于【缺料表】前面。
2. 只查询当前组织和业务类型 `PM055` 的采购订单数据。
3. 只处理采购订单行状态 `0/1/2` 的数据。
4. 正确计算 `ActualReqQty - IssuedQty`。
5. `SupplyWh` 有值时按确认的库存口径扣减；为空时不扣减。
6. 只将最终欠料数量大于 0 的数据纳入欠料清单。
7. 正确定位当前组织单据类型 `QT01` 的目标生产订单。
8. 委外欠料表能展示统计结果、导出 Excel，并提供【写入工单备料】按钮。
9. 点击写入按钮后，先清空目标工单原备料，再新增料号和 `ActualReqQty = 欠料数量`。
10. 第一张目标工单不可修改时直接报错，不查找下一张。
11. 部分保存、无数据提示、最近更新时间和日志行为符合确认结果。
