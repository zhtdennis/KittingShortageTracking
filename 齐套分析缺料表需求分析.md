# 齐套分析缺料表需求分析

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 功能名称 | 齐套分析缺料表 |
| 业务入口 | 生产制造 → 生产管理 → 齐套分析 |
| 目标页面 | `UFIDA::U9::MO::ManufactureSimu::SimuDoc` |
| 相关实体 | `UFIDA.U9.MO.ManufactureSimu.ItemWIPSimu`、`UFIDA.U9.MO.ManufactureSimu.DemandPick` |
| 当前状态 | 需求分析阶段，等待确认后再进入实现 |

## 2. 业务背景

齐套分析页面用于查看当前单据的可生产列表和可生产需求对象。业务上需要在该页面增加一个“缺料表”入口，用于把当前单据中存在缺料的需求明细集中展示出来，便于继续匹配采购订单和未审核采购收货单。

## 3. 功能目标

在齐套分析界面增加按钮【缺料表】。用户点击后，系统基于当前页面已加载的单据数据，读取 `UFIDA.U9.MO.ManufactureSimu.DemandPick` 的明细信息，筛选出缺料量大于 0 的记录，并按规则形成缺料明细列表。

缺料明细的处理顺序如下：

1. 先展示当前单据下缺料量 `> 0` 的 `DemandPick` 明细。
2. 明细按生产订单的计划开始日期正序排序。
3. 依据缺料数量，优先匹配未完的采购订单行数量。
4. 采购订单行匹配完成后，再继续匹配未审核的采购收货单行。

本次仅整理需求，不创建代码、不改动部署、不做系统修改。

## 4. 页面入口与操作范围

### 4.1 按钮位置

按钮增加在齐套分析页面工具栏，按钮名称为：

```text
缺料表
```

### 4.2 处理范围

- 以当前单据为处理对象。
- 只处理当前页面已经加载出来的单据/明细数据。
- 不扩展到其他未加载单据，不跨单据汇总。

### 4.3 数据来源范围

按钮点击后需要读取以下实体语义：

- `UFIDA::U9::MO::ManufactureSimu::SimuDoc`
- `UFIDA.U9.MO.ManufactureSimu.ItemWIPSimu`
- `UFIDA.U9.MO.ManufactureSimu.DemandPick`

其中，`DemandPick` 的明细信息是缺料表的主要来源。

## 5. 缺料明细筛选规则

按当前单据下的 `DemandPick` 明细逐行处理：

| 条件 | 处理方式 |
| --- | --- |
| 缺料量 `<= 0` | 不进入缺料表 |
| 缺料量 `> 0` | 进入缺料表 |
| 记录无效或关键字段为空 | 跳过或标记为待确认 |

缺料量判断应使用实体中的业务字段，不使用界面文本做模糊判断。

## 6. 排序规则

缺料表的明细按生产订单计划开始日期正序排序：

```text
计划开始日期 升序
```

如果同一日期存在多条记录，后续可按单据行号、物料编码或其他稳定键做次级排序；当前阶段先确认主排序规则。

## 7. 缺料匹配规则

### 7.1 需求优先级

每条缺料明细先按缺料数量进行分配，遵循以下顺序：

1. 先匹配未完的采购订单行数量。
2. 若采购订单行仍无法覆盖缺料数量，再匹配未审核的采购收货单行。

### 7.2 采购订单行匹配

对缺料量逐项匹配未完的采购订单行，匹配后需要体现：

- 采购订单号
- 采购订单行要求交货日期

匹配逻辑按缺料数量和未完订单行数量进行扣减，直到缺料被覆盖或可用采购订单行耗尽。

已确认的“未完”口径如下：

- 采购订单行状态在 `0、1、2` 之中。
- 采购订单行计划行的 `DeficiencyQtyTU` 参与加总。
- 以加总后的欠交数量作为可匹配数量来源。

### 7.3 未审核采购收货单行匹配

采购订单行匹配结束后，再将剩余缺料量与未审核的采购收货单行进行匹配。该部分仍按数量匹配，最终用于补充缺料来源明细。

已确认的“未审核”口径如下：

- 收货单头实体：`UFIDA::U9::PM::Rcv::Receivement`
- 收货行实体：`UFIDA.U9.PM.Rcv.RcvLine`
- 收货行状态不等于 `5`
- `SplitFlag` 不等于 `1`
- 满足上述条件的收货行纳入候选匹配范围

### 7.4 待确认点

以下规则当前先列为待确认：

- 采购订单“未完”的精确定义。
- 采购收货单“未审核”的状态判断口径。
- 是否允许一条缺料明细匹配多条采购订单行/收货单行。
- 数量扣减后是展示剩余缺料，还是展示已匹配数量与未匹配数量。

## 8. 展示字段

先按主要字段展示，后续再补充细列。结合当前界面截图，首版建议包含：

- 生产订单
- 生产料号
- 生产名称
- 备料料号
- 备料品名
- 实际需求量
- 计划开工日期
- 需求日期
- 缺料量

后续扩展项先记录为待追加字段：

- 匹配类型：采购订单 / 采购收货
- 供应单号：采购订单号 + 行号 / 收货单号 + 行号
- 供应日期：采购订单取采购订单行最大交期，采购收货取收货单单据日期

## 9. 交互与结果呈现

点击【缺料表】后，系统应基于当前单据生成缺料明细结果，并在页面上以表格或弹窗方式展示。

已确认采用页面内表格展示。

## 10. 异常与边界

- 当前单据没有任何缺料量大于 0 的明细时，应明确提示“无缺料数据”或同类提示。
- 当前单据数据未加载完整时，只处理已加载部分，并在需求上明确说明边界。
- 若缺少关键实体字段或状态判断不明确，先记录为需求待确认项，不直接进入实现假设。

## 11. 验收口径

需求确认后，后续实现阶段至少需要满足：

1. 齐套分析页面存在【缺料表】按钮。
2. 点击后能读取当前单据的 `DemandPick` 明细。
3. 只保留缺料量 `> 0` 的记录。
4. 明细按生产订单计划开始日期正序排序。
5. 能按顺序匹配未完采购订单行，再匹配未审核采购收货单行。
6. 首版先展示主要字段，后续按确认结果补列。

## 12. 待你确认的点

已确认完成，进入实现前不再保留待确认项。
